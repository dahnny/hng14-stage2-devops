# This project does not require any root-level secrets to run right now.
#
# Docker Compose now keeps normal app configuration inside docker-compose.yml.
#
# If you add real secrets later, put only those here.
# Examples:
# API_TOKEN=
# SENTRY_DSN=